
import React from 'react';

const CruelBrand: React.FC = () => {
  // Branding has been moved to the App Header inline.
  // Returning null to remove the old floating watermark.
  return null;
};

export default CruelBrand;
